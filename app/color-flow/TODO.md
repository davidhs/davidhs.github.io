# To-do

- [ ] Use canvas element and use dithering between colors. Dithering should be stable between resizing and time.
  - In particular, use "temporal" dithering between frames. It is sometimes very noticable the color shift, kind of like (spatial) color banding, here we've got temporal "spatial banding".
