# To-do

- [ ] Create a Google-Maps-like "memory" of different rendered frames at different scales. So a feature we have now when we zoom in we use the previous frame to fill in the new frame, but blurry, until a newly high resolution frame is rendered. If we the zoom out then it would be useful if we remembered what we rendered before at that scale and display that instead of waiting the frame to render again.
